target

# Profiling / perf outputs (see Makefile: perf, cachegrind, flamegraph, pgo)
/perf.data
/cachegrind.out
/flamegraph.svg
*.profraw
*.profdata

# GitHub Actions logs downloaded locally
logs_*.zip
